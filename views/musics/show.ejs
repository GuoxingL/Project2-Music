<%- include('../partials/header') %>

<section id="show-page">
  <div>Title: </div>
  <div><%= music.title %></div>
  <div>Release Year: </div>
  <div><%= music.releaseYear %></div>
  <div>Style: </div>
  <div><%= music.style %></div>
  <div>Singer: </div>
  <div><%= music.singer %></div>
  <div>Quotes: </div>
  <div><%= music.quotes %></div>
  <div>Now Showing: </div>
  <div><%= music.nowShowing ? 'Yes' : 'Nope' %></div>
 
  <div>Location:</div>
  <ul>
    <%- music.location.map(p => 
      `<li>${l.name} <small>${l.concertDate.toLocaleDateString()}</small></li>`
    ).join('') %>
  </ul>

</section>
<form id="add-per-to-location" action="/musics/<%= music._id %>/locations" method="POST">
  <select name="locationId">
  
    <%- locations.map(l => 
      `<option value="${l._id}">${l.name}</option>`
    ).join('') %>
  </select>
  <button type="submit">Add to Location</button>
</form>

<br><br><h2>Reviews</h2>
<form id="add-review-form" method="POST"
  action="/musics/<%= music._id %>/reviews">
  <label>Review:</label>
  <textarea name="content"></textarea>
  <label>Rating:</label>
  <select name="rating">
    <option value="1">1</option>
    <option value="2">2</option>
    <option value="3">3</option>
    <option value="4">4</option>
    <option value="5" selected>5</option>
  </select>
  <input type="submit" value="Add Review">
</form>

<% if (music.reviews.length) { %>
  <table>
    <thead>
      <tr>
        <th>User</th>
        <th>Date</th>
        <th>Review</th>
        <th>Rating</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% let total = 0 %>
      <% music.reviews.forEach(function(r) { %>
        <% total += r.rating %>
        <tr>
          <td class="review-user"><img alt="avatar" src="<%= r.userAvatar %>" referrerpolicy="no-referrer" ><%= r.userName %></td>
          <td><%= r.createdAt.toLocaleDateString() %></td>
          <td><%= r.content %></td>
          <td><%= r.rating %></td>
          <td>
            <% if (user?._id.equals(r.user)) { %>
              <form action="/reviews/<%= r._id %>?_method=DELETE" method="POST">
                <button type="submit">X</button>
              </form>
            <% } %>
          </td>
        </tr>
      <% }); %>
      <tr>
        <td colspan="3"></td>
        <td><strong><%= (total / music.reviews.length).toFixed(1) %></strong></td>
      </tr>
    </tbody>
  </table>
<% } else { %>
  <h5>No Reviews Yet</h5>
<% } %>
  
<%- include('../partials/footer') %>
